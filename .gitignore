# macOS
.DS_Store

# 机器本地的 Claude Code 设置（每台电脑各自的权限/路径，不要同步）
.claude/settings.local.json

# Playwright 测试工具依赖（本地安装，不同步）
.claude/skills/run-durian-dashboard/node_modules/

# Firebase 服务账号密钥（真正的密钥，能绕过所有 Firestore 安全规则，绝不能提交！）
.claude/firebase-service-account.json
.claude/scripts/node_modules/
